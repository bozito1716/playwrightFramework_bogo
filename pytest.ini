#- Is a configuration file for pytest, a testing framework for Python. It allows you to specify various settings and options for running tests.


[pytest]

addopts = --headed --browser chromium --slowmo=200 --html=reports/report.html --self-contained-html

testpaths = tests

markers =
    smoke: critical tests that validate the application after deployment
    regression: complete test suite to verify existing functionality